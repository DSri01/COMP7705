# Upload container image

- **`.tar` files only**, **one upload** per image slot.
- Upload is available only while file status is **awaiting upload**; otherwise use **New image** for a new slot.
- After upload, return to the component page and **Scan current image**.
