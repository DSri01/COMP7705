# Disabled CVEs

- CVEs **suppressed** on the current image (same data as the disabled section on the component page).
- Table: CVE, severity, source, disable badge, **Enable** shortcut.
- **Enable** or open image-CVE detail from the component page **View** link for full context.
