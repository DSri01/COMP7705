# Create CVE

- Register a canonical **CVE id** (e.g. `CVE-2021-44228`) in the global catalog.
- Required before agent web-fetch saves or manual linking via API; **Add CVEs** on a component can register missing ids automatically.
- Scans can also introduce CVEs without a separate create step.
