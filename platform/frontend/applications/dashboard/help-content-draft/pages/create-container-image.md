# New container image

- Adds the next slot in the image **chain** (CVEs from the previous image are **copied** if one exists).
- **Create new image** — adds a placeholder and opens **image details**.
- Next: **upload** a `.tar` from image details or the upload page, then **scan** from the component page.
